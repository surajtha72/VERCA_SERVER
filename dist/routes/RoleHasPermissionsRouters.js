"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.roleHasPermissionsRouter = void 0;
const express = __importStar(require("express"));
const RoleHasPermissionsController = __importStar(require("../controllers/RoleHasPermissionsController"));
const Authorization_1 = require("../Authorization");
let roleHasPermissionsRouter = express.Router();
exports.roleHasPermissionsRouter = roleHasPermissionsRouter;
/**
 * @swagger
 * tags:
 *   name: Role Has Permissions
 *   description: Endpoints for Role Has Permissions management
 */
/**
 * @swagger
 * /api/v1/role_has_permissions:
 *   get:
 *     tags: [Role Has Permissions]
 *     summary: Retrieve All Role Has Permissions
 *     description: Retrieve all Role Has Permissions.
 *     responses:
 *       '200':
 *         description: Successful response
 *         content:
 *           application/json:
 *             schema:
 *               type: object
 *               properties:
 *                 status:
 *                   type: number
 *                   example: 200
 *                 message:
 *                   type: string
 *                   example: "Success"
 *                 data:
 *                   type: array
 *                   items:
 *       '500':
 *         description: Internal Server Error
 *         content:
 *           application/json:
 *             schema:
 *               type: object
 *               properties:
 *                 status:
 *                   type: number
 *                   example: 500
 *                 message:
 *                   type: string
 *                   example: "Internal Server Error"
 *                 data:
 *                   type: object
 *                   example: null
 */
roleHasPermissionsRouter.get("/", Authorization_1.VerifyToken, RoleHasPermissionsController.GetAllRoleHasPermissions);
